const LoadClaimsPage = () => {
  return sampleComponent('load claims page')
}

const sampleComponent = (text: any) => {
  return (
    <div>
      <p>{text}</p>
    </div>
  )
}

export {LoadClaimsPage, sampleComponent}
